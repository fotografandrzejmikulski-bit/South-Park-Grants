# Results

This directory contains generated experiment summaries.

Do not add hand-written claims of performance. Each result should identify the exact experiment configuration, commit SHA, model/checkpoint, seed, metrics, hardware, and limitations.

Suggested record fields:

```text
experiment_id:
commit:
model:
checkpoint:
layers:
seed:
config:
metrics:
compute:
latency:
notes:
limitations:
```
